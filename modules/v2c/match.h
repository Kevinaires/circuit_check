/* match.h */

#ifndef __match_h
#define __match_h

int match(char *p, char *s);
/* p = pattern to match
   s = string to match it to
   Compares the pattern p with the string s and returns true if they match,
   false if they don't or if there is a syntax error in the pattern. */

#endif

/* End of match.h */
